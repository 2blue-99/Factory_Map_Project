<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Factory_Map_Project" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="bottomSheetDialogTheme">@style/DialogStyle</item>
    </style>

    <style name="Theme.Factory_Map_Project" parent="Base.Theme.Factory_Map_Project" />

    <style name="DialogStyle" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="bottomSheetStyle">@style/CustomRoundedBackgroundBottomSheet</item>
    </style>

    <style name="CustomRoundedBackgroundBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_background</item>
    </style>

    <style name="CustomAlertDialog" parent="ThemeOverlay.Material3.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/CustomDialogPositiveButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/CustomDialogNegativeButton</item>
    </style>

    <style name="CustomDialogPositiveButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/primary</item> <!-- 버튼 텍스트 색상 -->
    </style>

    <style name="CustomDialogNegativeButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/black</item> <!-- 버튼 텍스트 색상 -->
    </style>
</resources>